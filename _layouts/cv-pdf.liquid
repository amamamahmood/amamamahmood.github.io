<article>
  {% if page.cv_pdf %}
    <script>
      window.open(
        "{{ page.cv_pdf | prepend: 'assets/pdf/' | relative_url }}",
        "_blank"
      );
    </script>

    <p>
      Your CV should open in a new tab automatically.
      If not,
      <a
        href="{{ page.cv_pdf | prepend: 'assets/pdf/' | relative_url }}"
        target="_blank"
        rel="noopener noreferrer"
      >
        click here to view the PDF
      </a>.
    </p>
  {% else %}
    <p>CV PDF not found. Please set <code>cv_pdf</code> in front matter.</p>
  {% endif %}
</article>
